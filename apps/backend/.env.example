DJANGO_SECRET_KEY=change-me
DEBUG=true
DATABASE_URL=postgres://postgres:postgres@db:5432/fitness
ALLOWED_HOSTS=localhost,127.0.0.1
SENTRY_DSN=

# Email (leave EMAIL_BACKEND unset locally to print emails to the console).
# For real sending (Gmail App Password, or any SMTP provider):
#   EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=
USE_CLOUDFLARE_CLIENT_IP=